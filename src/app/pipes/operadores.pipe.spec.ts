import { OperadoresPipe } from './operadores.pipe';

describe('OperadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new OperadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
